interface:
  display_name: "Story Factory"
  short_description: "Create stories with persistent memory."
  default_prompt: "Use $story-factory to brainstorm, plan, write, and save a story with King Context memory."
policy:
  allow_implicit_invocation: true
