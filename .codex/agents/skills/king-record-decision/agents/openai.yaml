interface:
  display_name: "King Record Decision"
  short_description: "Create and validate ADRs."
  default_prompt: "Use $king-record-decision to record this architecture decision as an ADR."
policy:
  allow_implicit_invocation: true
