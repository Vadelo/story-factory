{
  "title": "`Annotations`",
  "path": "specification-draft-schema-annotations",
  "url": "https://modelcontextprotocol.io/specification/draft/schema",
  "keywords": [
    "Annotations",
    "Role",
    "audience",
    "priority",
    "lastModified",
    "ISO 8601",
    "Cursor",
    "EmptyResult",
    "pagination",
    "opaque token"
  ],
  "use_cases": [
    "Configure when data requires prioritization for server operations",
    "Implement when defining target user roles like user or assistant",
    "Use when tracking resource modification history via ISO 8601 timestamps",
    "Implement when handling paginated API responses using opaque tokens",
    "Use when a request succeeds but returns no data payload"
  ],
  "tags": [
    "api-reference",
    "data-structures",
    "metadata"
  ],
  "priority": 7,
  "content": "interface Annotations {\n   audience?: Role [];\n   priority?: number;\n   lastModified?: string;\n}Optional annotations for the client. The client can use annotations to inform how objects are used or displayed audience?: Role[] Describes who the intended audience of this object or data is. It can include multiple entries to indicate content useful for multiple audiences (e.g., `\\[\"user\", \"assistant\"]`). priority?: number Describes how important this data is for operating the server. A value of 1 means \"most important,\" and indicates that the data is\neffectively required, while 0 means \"least important,\" and indicates that\nthe data is entirely optional. lastModified?: string The moment the resource was last modified, as an ISO 8601 formatted string. Should be an ISO 8601 formatted string (e.g., \"2025-01-12T15:00:58Z\"). Examples: last activity timestamp in an open file, timestamp when the resource\nwas attached, etc.\n\nCursor: stringAn opaque token used to represent a cursor for pagination.\n\nEmptyResult: Result A result that indicates success but carries no data.",
  "token_estimate": 208
}